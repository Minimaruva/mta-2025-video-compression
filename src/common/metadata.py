from dataclasses import dataclass

@dataclass
class Metadata:
    #FIXME
    #Add any or remove as many metadata as you want
    #This are just for example
    scale: float = 30.0
    offset: float = 30.0